xs = []

[1, 2, 3].Each() \ x
  xs.Push(x + 2)

Assert(xs == [3, 4, 5], "xs == [3, 4, 5]")


looped = False

[[1, 2]].Each() \\ [a, b]
  looped = True
  Assert([a, b] == [1, 2], "[a, b] == [1, 2]")

# Make sure that a bug in Each doesn't this test to pass silently.
Assert(looped, "looped")
