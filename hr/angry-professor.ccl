lines = SplitLines(Read())
i = 1
while i < Size(lines)
	[N, K] = Map(Int, Split(lines[i]))
	a = Map(Int, Split(lines[i+1]))
	total = FoldLeft(\total x. if x <= 0; total+1 else total, 0, a)
	Print(if total < K; "YES" else "NO")
	i = i+2
